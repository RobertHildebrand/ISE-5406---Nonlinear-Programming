name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read
  pages: write
  id-token: write

concurrency:
  group: pages
  cancel-in-progress: false

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: visualizations/package-lock.json

      - name: Install visualization deps
        working-directory: visualizations
        # `npm install` instead of `npm ci` to side-step the
        # optional-dependencies lock-file bug
        # (https://github.com/npm/cli/issues/4828) where the
        # platform-specific rollup binary for the runner's OS is
        # missing from a lock file generated on a different OS.
        run: npm install --no-audit --no-fund

      - name: Build visualizations
        working-directory: visualizations
        run: npm run build

      - name: Stage site
        run: |
          mkdir -p _site
          rsync -a \
            --exclude='.git' \
            --exclude='.github' \
            --exclude='visualizations' \
            --exclude='_site' \
            --exclude='.DS_Store' \
            ./ _site/
          mkdir -p _site/visualizations
          cp -R visualizations/dist/. _site/visualizations/

      - uses: actions/configure-pages@v5
      - uses: actions/upload-pages-artifact@v3
        with:
          path: _site

  deploy:
    needs: build
    runs-on: ubuntu-latest
    environment:
      name: github-pages
      url: ${{ steps.deployment.outputs.page_url }}
    steps:
      - id: deployment
        uses: actions/deploy-pages@v4
